<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.szhuima.agent.infrastructure.mapper.AiAgentTaskScheduleMapper">

    <resultMap id="dataMap" type="dev.szhuima.agent.infrastructure.po.AiAgentTaskSchedule">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="agentId" column="agent_id" jdbcType="BIGINT"/>
            <result property="taskName" column="task_name" jdbcType="VARCHAR"/>
            <result property="description" column="description" jdbcType="VARCHAR"/>
            <result property="cronExpression" column="cron_expression" jdbcType="VARCHAR"/>
            <result property="taskParam" column="task_param" jdbcType="VARCHAR"/>
            <result property="status" column="status" jdbcType="TINYINT"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,agent_id,task_name,
        description,cron_expression,task_param,
        status,create_time,update_time
    </sql>

    <!-- 查询所有任务调度配置 -->
    <select id="queryAllTaskSchedule" resultMap="dataMap">
        SELECT id, agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time
        FROM ai_agent_task_schedule
        ORDER BY id
    </select>

    <!-- 根据ID查询任务调度配置 -->
    <select id="queryTaskScheduleById" parameterType="java.lang.Long" resultMap="dataMap">
        SELECT id, agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time
        FROM ai_agent_task_schedule
        WHERE id = #{id}
    </select>

    <!-- 根据智能体ID查询任务调度配置 -->
    <select id="queryTaskScheduleByAgentId" parameterType="java.lang.Long" resultMap="dataMap">
        SELECT id, agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time
        FROM ai_agent_task_schedule
        WHERE agent_id = #{agentId}
    </select>

    <!-- 根据条件查询任务调度配置列表 -->
    <select id="queryTaskScheduleList" parameterType="AiAgentTaskSchedule" resultMap="dataMap">
        SELECT
        id, agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time
        FROM
        ai_agent_task_schedule
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="agentId != null">
                AND agent_id = #{agentId}
            </if>
            <if test="taskName != null and taskName != ''">
                AND task_name LIKE CONCAT('%', #{taskName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        <if test="pageSize != null and pageSize > 0">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <!-- 查询所有有效的任务调度配置 -->
    <select id="queryAllValidTaskSchedule" resultMap="dataMap">
        SELECT id, agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time
        FROM ai_agent_task_schedule
        WHERE status = 1
        ORDER BY id
    </select>

    <!-- 查询无效任务ID -->
    <select id="queryAllInvalidTaskScheduleIds" resultType="java.lang.Long">
        SELECT id from ai_agent_task_schedule
        WHERE status = 0
    </select>

    <!-- 插入任务调度配置 -->
    <insert id="insert" parameterType="AiAgentTaskSchedule">
        INSERT INTO ai_agent_task_schedule (
            agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time
        ) VALUES (
                     #{agentId}, #{taskName}, #{description}, #{cronExpression}, #{taskParam}, #{status}, #{createTime}, #{updateTime}
                 )
    </insert>

    <!-- 更新任务调度配置 -->
    <update id="update" parameterType="AiAgentTaskSchedule">
        UPDATE ai_agent_task_schedule
        SET agent_id = #{agentId},
            task_name = #{taskName},
            description = #{description},
            cron_expression = #{cronExpression},
            task_param = #{taskParam},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>
</mapper>
