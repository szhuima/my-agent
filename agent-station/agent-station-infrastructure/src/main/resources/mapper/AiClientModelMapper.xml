<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.szhuima.agent.infrastructure.mapper.AiClientModelMapper">

    <resultMap id="dataMap" type="dev.szhuima.agent.infrastructure.po.AiClientModel">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="modelApiName" column="model_api_name" jdbcType="VARCHAR"/>
        <result property="baseUrl" column="base_url" jdbcType="VARCHAR"/>
        <result property="apiKey" column="api_key" jdbcType="VARCHAR"/>
        <result property="completionsPath" column="completions_path" jdbcType="VARCHAR"/>
        <result property="embeddingsPath" column="embeddings_path" jdbcType="VARCHAR"/>
        <result property="modelSource" column="model_source" jdbcType="VARCHAR"/>
        <result property="modelType" column="model_type" jdbcType="VARCHAR"/>
        <result property="modelName" column="model_name" jdbcType="VARCHAR"/>
        <result property="timeout" column="timeout" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="TINYINT"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>


    <!-- 根据ID查询模型配置 -->
    <select id="queryModelConfigById" parameterType="java.lang.Long" resultMap="dataMap">
        SELECT *
        FROM ai_client_model
        WHERE id = #{id}
    </select>

    <!-- 根据模型名称查询模型配置 -->
    <select id="queryModelConfigByName" parameterType="java.lang.String" resultMap="dataMap">
        SELECT *
        FROM ai_client_model
        WHERE model_name = #{modelName}
    </select>


    <!-- 根据ID删除模型配置 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE
        FROM ai_client_model
        WHERE id = #{id}
    </delete>

    <!-- 根据客户端ID列表查询模型配置 -->
    <select id="queryModelConfigByClientIds" parameterType="java.util.List" resultMap="dataMap">
        SELECT m.*
        FROM ai_client_model m
        WHERE m.id IN (
        SELECT DISTINCT c.model_id
        FROM ai_client c
        WHERE c.id IN
        <foreach collection="list" item="clientId" open="(" separator="," close=")">
            #{clientId}
        </foreach>
        )
    </select>

    <select id="queryClientModelList" parameterType="dev.szhuima.agent.infrastructure.po.AiClientModel"
            resultMap="dataMap">
        SELECT
        *
        FROM
        ai_client_model
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="modelName != null and modelName != ''">
                AND model_name LIKE CONCAT('%', #{modelName}, '%')
            </if>
            <if test="modelType != null and modelType != ''">
                AND model_type = #{modelType}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        <if test="pageSize != null and pageSize > 0">
            LIMIT #{offset}, #{limit}
        </if>
    </select>
</mapper>
